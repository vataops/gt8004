# ============================================================
# Companion Agent — Shared Environment Variables
# ============================================================
# 이 파일을 .env로 복사한 뒤 값을 채워주세요.
# deploy/, erc8004/, register/ 스크립트가 모두 이 파일을 참조합니다.
# ============================================================

# --- LLM ---
OPENAI_API_KEY=sk-...
LLM_MODEL=gpt-4o

# --- Agent Metadata (Agent Card + On-chain 등록에 공통 사용) ---
AGENT_NAME="Companion-Agent"
AGENT_DESCRIPTION="General-purpose LLM agent supporting chat, summarization, translation, and code assistance via A2A protocol"
AGENT_VERSION=1.0.0

# --- Agent URL (Cloud Run 배포 후 설정) ---
AGENT_URL=

# --- Server ---
PORT=8080
LOG_LEVEL=info

# --- ERC-8004 On-chain Registration (erc8004/mint.py) ---
EVM_PRIVATE_KEY=0x
BASE_SEPOLIA_RPC=https://base-sepolia-rpc.publicnode.com
IDENTITY_REGISTRY=0x8004A818BFB912233c491871b3d84c89A494BD9e
CHAIN_ID=84532

# --- GT8004 Platform Registration (register/register.py) ---
REGISTRY_URL=http://localhost:8080
ERC8004_TOKEN_ID=

# --- GCP Cloud Run Deployment (deploy/deploy.sh) ---
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
SERVICE_NAME=ael-companion-agent
