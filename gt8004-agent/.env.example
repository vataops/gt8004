# ============================================================
# GT8004 Platform Agent — Environment Variables
# ============================================================

# --- GT8004 API Gateway ---
GT8004_API_URL=https://api.gt8004.xyz
# GT8004_API_URL=http://localhost:8080  # for local dev

# --- Server ---
PORT=8080
LOG_LEVEL=info

# --- Agent Identity ---
AGENT_NAME=GT8004 Platform Agent
AGENT_VERSION=1.0.0
AGENT_URL=                            # public A2A URL (for agent card)

# --- LLM (A2A only) ---
GOOGLE_API_KEY=                       # Google AI Studio API key
LLM_MODEL=gemini-2.0-flash

# --- x402 Payment (A2A only) ---
X402_PAY_TO=                          # wallet address to receive USDC
X402_NETWORK=base
X402_PRICE=0.01

# --- A2A Bridge (MCP → A2A internal proxy) ---
A2A_BASE_URL=                         # e.g., https://gt8004-a2a-xxxx.run.app
INTERNAL_API_KEY=                     # shared secret between MCP ↔ A2A

# --- Provider Info (agent card) ---
PROVIDER_ORG=
PROVIDER_URL=

# --- GT8004 SDK (Platform Agent's own telemetry) ---
GT8004_AGENT_ID=
GT8004_API_KEY=
GT8004_INGEST_URL=https://ingest.gt8004.xyz/v1/ingest

# --- GCP Deployment ---
GCP_PROJECT_ID=
GCP_REGION=us-central1
