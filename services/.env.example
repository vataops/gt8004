# ── Network ────────────────────────────────────────────
NETWORK_MODE=testnet                # testnet | mainnet

# ── RPC Endpoints ─────────────────────────────────────
# BASE_RPC_URL=https://base-mainnet.core.chainstack.com/YOUR_KEY
# IDENTITY_REGISTRY_RPC=https://ethereum-rpc.publicnode.com

# ── Discovery Service — Sync Tuning ──────────────────
SCAN_SYNC_INTERVAL=1800             # Main sync loop interval (seconds, default 1800 = 30min)
BACKFILL_INTERVAL=1800              # Backfill loop interval (seconds, default 1800 = 30min)
BACKFILL_WORKERS=3                  # Concurrent backfill goroutines (default 3)
BACKFILL_BATCH_SIZE=200             # Tokens per backfill cycle (default 200)
REPUTATION_INTERVAL=3600            # Reputation refresh interval (seconds, default 3600 = 1hr)
RESOLVE_WORKERS=10                  # Concurrent resolve goroutines for ownership+URI (default 10)

# ── Security ──────────────────────────────────────────
# INTERNAL_SECRET=dev-secret

# ── Ingest Service ────────────────────────────────────
# INGEST_WORKERS=4
# INGEST_BUFFER_SIZE=1000
# MAX_BODY_SIZE_BYTES=51200
